# 试卷成绩上报接口 · 对接说明

> 交付对象：服务器负责人
> 来源：PWA 学习中心「交互式 HTML 试卷」（由 zhongyi-zonghe skill 的 `build_exam_html.py` 生成）
> 日期：2026-09-05

---

## 一、背景

上传到 PWA 学习中心的交互式 HTML 试卷，学生填写姓名作答、点击「提交试卷·查看成绩」后，页面在**本地原地批改并显示成绩**的同时，会自动向服务器发送一份**结构化成绩 JSON**。服务器只需新增一个接口接收并落库，即可记录所有学生的答题成绩。

**试卷 HTML 是纯静态文件，不需要服务器参与判分**——判分在浏览器端完成，服务器只做成绩接收。

---

## 二、接口约定

```
POST /api/exam/submit
Content-Type: application/json
```

- 路径可在生成试卷时自定义（`--submit-url` 参数），**默认为 `/api/exam/submit`**；如需更换路径，请告知出题方重新生成试卷即可
- 试卷与 PWA 同源部署，**不涉及跨域（CORS）问题**
- 发送方式：页面优先用 `navigator.sendBeacon`，不可用时降级为 `fetch(keepalive)`；两种方式的 body 完全相同
- **响应要求：返回任意 2xx 即视为上报成功**（如 `{"ok": true}`）；返回 4xx/5xx 或网络失败时，页面会提示学生"上报失败、已本地留档"，不影响学生查看成绩

### exam_id 在试卷 HTML 中的位置

每份试卷生成时，`exam_id` 已写入三个位置，服务器端或人工均可直接识别某份 HTML 对应哪张卷：

```html
<!DOCTYPE html>
<!-- exam_id: exam-61d5aed2fd | submit_url: /api/exam/submit | ... -->
<html lang="zh-CN">
<head>
<meta name="exam-id" content="exam-61d5aed2fd">
<meta name="exam-submit-url" content="/api/exam/submit">
<meta name="exam-total-score" content="100">
...
```

1. 文件开头的 HTML 注释（`<!-- exam_id: ... -->`）
2. `<head>` 中的 meta 标签：`exam-id`、`exam-submit-url`、`exam-total-score`
3. 页面内嵌 JS 的 `const EXAM = {...}` 数据对象（运行时上报即取自此）

如需在服务端扫描已上传的试卷建立"试卷清单"，直接解析 meta 标签即可。注意 `exam_id` 平时不会显示在试卷页面上，只在学生提交后的 POST body 中发给服务器。

---

## 三、请求 body 字段说明

顶层对象：

| 字段 | 类型 | 说明 |
|------|------|------|
| `type` | string | 固定值 `"exam_result"`，服务器以此识别成绩上报请求 |
| `version` | number | 上报格式版本，当前为 `1` |
| `exam_id` | string | 试卷唯一 ID，如 `exam-dc6aefb456`（同一份试卷所有学生相同；未指定时按卷名自动生成） |
| `exam_title` | string | 试卷名称 |
| `student_name` | string | 学生姓名（卷首填写，提交时必填校验，不会为空） |
| `submitted_at` | string | 提交时间，ISO 8601（UTC），如 `2026-09-05T02:30:00.000Z` |
| `total_score` | number | 试卷满分 |
| `final_score` | number | 总得分 = `objective_score` + `subjective_self` |
| `objective_score` | number | 客观题得分（浏览器自动批改） |
| `objective_max` | number | 客观题满分 |
| `subjective_self` | number | 主观题自评分（学生对照参考答案自评） |
| `subjective_max` | number | 主观题满分 |
| `answered_count` | number | 已答题数 |
| `question_count` | number | 总题数 |
| `correct_count` / `objective_count` | number | 客观题答对数 / 客观题总数 |
| `accuracy` | number | 客观题正确率（百分数 0–100） |
| `answers` | array | 逐题明细，见下表 |

`answers[]` 每个元素：

| 字段 | 类型 | 说明 |
|------|------|------|
| `qid` | number | 题号（从 1 起） |
| `type` | string | `single` 单选 / `multiple` 多选 / `fill` 填空 / `short` 简答 |
| `max_score` | number | 该题满分 |
| `score` | number | 该题得分 |
| `answer` | any | 学生作答：单选/填空为索引或文本，多选为索引数组，简答为文本，未答为 `null` |
| `correct` | bool\|null | 是否答对；简答题恒为 `null`（自评无对错） |
| `self_score` | number | 仅简答题有：自评分 |

### 完整示例

```json
{
  "type": "exam_result",
  "version": 1,
  "exam_id": "exam-dc6aefb456",
  "exam_title": "中医综合·胸痹专题演示卷",
  "student_name": "张三",
  "submitted_at": "2026-09-05T02:30:00.000Z",
  "total_score": 22,
  "final_score": 15,
  "objective_score": 10,
  "objective_max": 12,
  "subjective_self": 5,
  "subjective_max": 10,
  "answered_count": 6,
  "question_count": 7,
  "correct_count": 3,
  "objective_count": 6,
  "accuracy": 50,
  "answers": [
    { "qid": 1, "type": "single",   "max_score": 2, "score": 2, "answer": 1,        "correct": true },
    { "qid": 2, "type": "single",   "max_score": 2, "score": 0, "answer": 0,        "correct": false },
    { "qid": 3, "type": "multiple", "max_score": 3, "score": 0, "answer": [0],      "correct": false },
    { "qid": 5, "type": "fill",     "max_score": 2, "score": 0, "answer": "错误答案", "correct": false },
    { "qid": 7, "type": "short",    "max_score": 8, "score": 8, "answer": "阳微指…",  "correct": null, "self_score": 8 }
  ]
}
```

---

## 四、服务器端最小实现

### Node.js / Express

```js
app.post("/api/exam/submit", express.json({ limit: "256kb" }), (req, res) => {
  const r = req.body;
  if (r.type !== "exam_result") return res.status(400).json({ error: "bad type" });
  if (!r.exam_id || !r.student_name) return res.status(400).json({ error: "missing fields" });

  db.saveExamResult(r);   // 建议唯一键：exam_id + student_name + submitted_at
  res.json({ ok: true });
});
```

### Python / FastAPI（等价实现）

```python
from pydantic import BaseModel
from typing import Any, Optional

class ExamResult(BaseModel):
    type: str
    version: int
    exam_id: str
    exam_title: str
    student_name: str
    submitted_at: str
    total_score: float
    final_score: float
    answers: list[dict[str, Any]]
    # 其余统计字段按需声明或用 extra="allow"

@app.post("/api/exam/submit")
async def exam_submit(r: ExamResult):
    if r.type != "exam_result":
        raise HTTPException(400, "bad type")
    save_exam_result(r.model_dump())   # 落库
    return {"ok": True}
```

### 建议的落库表结构

| 列 | 说明 |
|----|------|
| `exam_id` | 试卷 ID |
| `student_name` | 学生姓名 |
| `submitted_at` | 提交时间 |
| `final_score` / `total_score` | 得分 / 满分 |
| `objective_score` / `subjective_self` | 客观分 / 自评分 |
| `accuracy` | 正确率 |
| `detail_json` | 整个 body 原样存 JSON，便于后续扩展分析 |

---

## 五、测试方法

1. 拿演示卷 `中医综合·胸痹专题演示卷.html`（与 PWA 同源部署或本地起服务）
2. 填姓名、答几题、点提交
3. 服务器日志应看到 `POST /api/exam/submit`，body 即上文示例结构

也可用 curl 模拟：

```bash
curl -X POST http://localhost:3000/api/exam/submit \
  -H "Content-Type: application/json" \
  -d '{"type":"exam_result","version":1,"exam_id":"exam-test","exam_title":"测试卷","student_name":"李四","submitted_at":"2026-09-05T02:30:00.000Z","total_score":10,"final_score":8,"objective_score":8,"objective_max":10,"subjective_self":0,"subjective_max":0,"answered_count":1,"question_count":1,"correct_count":1,"objective_count":1,"accuracy":100,"answers":[{"qid":1,"type":"single","max_score":10,"score":10,"answer":0,"correct":true}]}'
```

---

## 六、注意事项 / FAQ

1. **重复提交**：学生提交后按钮即锁定，正常不会重复上报；但页面刷新后可再次作答提交。建议服务器按 `exam_id + student_name` 做"保留最新一条"或"全部保留+时间戳"的策略，按业务定。
2. **离线/上报失败兜底**：上报失败时，页面会把完整成绩 JSON 存入浏览器 `localStorage`（key 格式 `exam_result:<exam_id>:<submitted_at>`）。如需离线补录，可在学生下次在线时由前端读取这些 key 补传。
3. **body 大小**：简答题答案为全文文本，单次上报一般在几十 KB 以内，`express.json({ limit: "256kb" })` 足够。
4. **安全提醒**：判分在浏览器端完成，成绩 JSON 是"学生本机批改结果"的上报，可满足学习场景记录需求；若未来需要防作弊的可信成绩，需将题库与判分移到服务器端（可与出题方另行规划）。
5. **自定义上报路径**：如服务器希望使用其他路径（如 `/api/study/exam-result`），告知出题方，用 `--submit-url` 参数重新生成试卷即可，无需改前端代码。
