# DailyRead 音频功能方案

> 目标：鸿蒙端打开阅读页后，若文章含音频数据则自动播放；Win 端配套 App 在文章增删改中支持音频的添加、删除、预览。

---

## 一、现状分析

三端当前**均无任何音频字段或处理逻辑**：

| 端 | 数据模型 | UI / 处理逻辑 |
|---|---|---|
| 鸿蒙端 | `Article` 仅有 `imagewebp`，无音频字段 | `Reader.ets` 解析正文、解码 WebP 图片、打卡，无播放组件；`ImageCompressionService` 仅为图片压缩 |
| Win 端 | 文章字段 `id/clientId/chineseChars/imagewebp/iscontent/createTime/lastModified`，无音频字段 | 编辑对话框只有图片区（选择/粘贴/移除图片），无音频控件 |
| 后端 | `articles` 表仅 `imagewebp LONGTEXT`，无音频字段 | 路由 `imagewebp` 内联在请求体存储，无 multer、无文件上传路由、无静态文件服务 |

---

## 二、音频推荐格式

### 推荐：m4a（AAC-LC 编码）

**参数建议**

| 参数 | 推荐值 |
|---|---|
| 容器 | m4a / mp4 audio |
| 编码 | AAC-LC（低复杂度 profile） |
| 比特率 | 64–96 kbps（语音朗读足够） |
| 采样率 | 44.1 kHz |
| 声道 | 单声道（朗读无需立体声） |
| 时长建议 | ≤ 3 分钟 / 条 |
| 体积估算 | 1 分钟约 0.5–0.7 MB，base64 后约 0.7–0.9 MB |

**选择理由**

1. **压缩率高**：相同音质下体积约为 mp3 的 70–80%、wav 的 1/10，base64 内嵌方案下数据库压力可控
2. **鸿蒙原生支持**：`AVPlayer` 直接支持 m4a/aac，无需第三方解码器
3. **Win 端可编码**：Qt Multimedia（`QMediaPlayer` + FFmpeg 后端）支持 AAC 编码与播放
4. **跨端兼容**：iOS/Android/Web 通用，未来扩展无障碍
5. **适合朗读场景**：人声语音在中低比特率下质量损失不明显，64–96 kbps 已足够

**不推荐格式**

| 格式 | 不推荐原因 |
|---|---|
| wav | 体积过大（1 分钟约 10 MB），base64 膨胀 33%，数据库与同步带宽压力极大 |
| mp3 | 压缩率不如 AAC，且历史专利问题；非鸿蒙首选推荐 |
| ogg | 鸿蒙端原生支持不确定，跨端兼容性差 |
| flac | 无损体积大，朗读场景无必要 |

**约束**

- Win 端录入时统一转码为 m4a 再 base64 存储，无论源文件是 mp3/wav/ogg
- 单条音频 base64 体积建议 **< 2 MB**（约 2–3 分钟朗读）
- MySQL `max_allowed_packet` 需调大到 **≥ 16 MB**

---

## 三、方案选型

| 维度 | 方案 A：base64 内嵌（推荐） | 方案 B：独立文件 + URL 引用 |
|---|---|---|
| 与现状一致性 | 与 `imagewebp` 完全一致，三端逻辑可复用 | 需新增 multer、文件上传路由、静态服务 |
| 改造量 | 小（加一个 LONGTEXT 字段） | 大（新增依赖、路由、文件存储管理） |
| 离线场景 | 天然支持（已内嵌） | 需联网下载，离线不可用 |
| 数据库膨胀 | 单条记录可能 3–5 MB | 数据库轻，但服务器需托管文件 |
| 同步带宽 | 大（随文章 payload 一起传） | 小（只传 URL） |
| 备份/迁移 | 沿用现有 JSON 流程 | 需额外处理文件资源 |

**采用方案 A**：与现有图片方案保持一致，三端改造最小，离线天然可用。配合"限制单条音频 < 2 MB + 调大 `max_allowed_packet` + 音频统一为 m4a/aac"即可规避主要缺点。

---

## 四、三端改造点

### 4.1 后端（dailyread-server）

- `schema.sql` 与 `src/db/init.js`：`articles` 表新增 `audiobase64 LONGTEXT` 字段
- `src/routes/articles.js`：字段映射、create/update/batch 路由接收存储 `audiobase64`
- `src/index.js`：确认 JSON body 大小限制足够（当前为图片已调大，音频更大需复核）
- 运维：`ALTER TABLE articles ADD COLUMN audiobase64 LONGTEXT` + 检查 `max_allowed_packet ≥ 16M`

### 4.2 Win 端（Dailyread_win_article_concept_manager）

- `article_concept_manager.py`：
  - `DataModel.add_article / update_article`：新增 `audiobase64` 字段（编辑时保留原值，遵循现有"保留原始字段"约束）
  - 编辑对话框 UI（L634–L791 区域）：在图片区下方新增**音频区**——选择音频按钮、播放器预览（`QMediaPlayer` / PyQt5 multimedia）、删除按钮、文件大小显示
  - 仿 `on_select_image / on_remove_image / update_image_preview` 新增 `on_select_audio / on_remove_audio / update_audio_preview`
  - 新增音频转码工具：将 mp3/wav 统一转码为 m4a 再 base64（保证鸿蒙 AVPlayer 兼容）
- `api_client.py`：`push_article / push_articles_batch` 字段透传
- `sync_service.py` 离线队列：字段透传
- `migration_dialog.py`：迁移时透传 `audiobase64`

### 4.3 鸿蒙端（Dailyread_Harmony）

- `model/Article.ets`：新增 `audiobase64?: string`
- `model/ApiTypes.ets`：`ArticlePayload` 同步新增
- 新增 `service/AudioService.ets`（参考 `ImageCompressionService.ets` 的 base64 解码模式）：base64 → 沙箱临时文件 → 返回路径
- `pages/Reader.ets`：
  - `aboutToAppear` 文章加载后，检测 `audiobase64` 非空 → 调用 AudioService 解码到沙箱 → 初始化 `AVPlayer` → `prepare()` 成功后 `play()` 自动播放
  - UI 新增播放控制条（播放/暂停、进度、时长），遵循 ArkTS DSL 约束（仅链式调用）
  - `aboutToDisappear`：`player.release()` + 删除临时文件
- `repository/ArticleRepository.ets`、`service/BackupService.ets`、`service/SyncService.ets`、`service/OfflineWriteQueue.ets`：字段透传

---

## 五、自动播放逻辑（鸿蒙阅读页）

1. 进入 `Reader.ets`，加载文章数据
2. `parseContent()` 后检测 `audiobase64` 是否非空
3. 非空：base64 解码 → 写入沙箱临时文件 → 创建 `AVPlayer` 设置 src → `prepare()` → `play()` 自动播放
4. UI 显示控制条，支持暂停/继续/拖动
5. 离开页面：`release()` 播放器 + 清理临时文件

---

## 六、风险与注意事项

1. **数据库膨胀**：单条文章（图片+音频）可能 3–5 MB，需评估文章总量
2. **同步性能**：弱网下大 payload 同步慢，建议 Win 端批量同步时分片或提示
3. **格式兼容**：Win 端统一转 m4a/aac，鸿蒙 AVPlayer 原生支持
4. **max_allowed_packet**：MySQL 默认 4 MB，需调大到 16 MB+
5. **字段保留约束**：Win 端编辑保存时必须保留 `audiobase64` 原值（与现有 `checkInDays/createTime` 保留约束一致），避免误清空
6. **clientId 同步**：跨端仍用 clientId 作为全局唯一标识，音频字段随文章一起同步
7. **ArkTS 语法约束**：阅读页播放控制 UI 必须遵循声明式 DSL（仅组件/属性链式调用，无裸露 TS 语句）
8. **JSON 序列化约束**：Win 端保存时音频 base64 序列化与写盘须在后台线程执行，避免主线程阻塞

---

## 七、落地步骤

1. **后端**：DB 迁移 + articles 路由字段扩展 + payload 限制复核
2. **Win 端**：DataModel 字段 + 编辑 UI 音频区 + 音频转码工具 + api/sync 透传
3. **鸿蒙端**：Article 模型 + AudioService + Reader 自动播放 + 备份/同步透传
4. **三端联调**：Win 端上传音频 → 后端存储 → 鸿蒙端阅读页自动播放
5. **历史数据兼容**：已有文章 `audiobase64` 为空，不影响显示与播放
